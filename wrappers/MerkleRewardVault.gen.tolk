// Auto-generated wrapper for contract 'MerkleRewardVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/tokenomics-messages"
import "@contracts/tokenomics-storage"

/// Storage `MerkleRewardStorage` is defined in `@contracts/tokenomics-storage`
struct MerkleRewardVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun MerkleRewardVault.fromStorage(
    storage: MerkleRewardStorage,
    toShard: AddressShardingOptions? = null,
): MerkleRewardVault {
    val stateInit = ContractState {
        code: build("MerkleRewardVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return MerkleRewardVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun MerkleRewardVault.fromAddress(address: address): MerkleRewardVault {
    return MerkleRewardVault { address };
}

/// Deploys the contract to the blockchain
fun MerkleRewardVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendCreateRewardBatch(
    self,
    from: address,
    batchId: uint64,
    root: uint256,
    totalHi: coins,
    metadataHash: uint256,
    poolType: uint8,
    requiredGrowthLevel: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CreateRewardBatch {
            batchId,
            root,
            totalHi,
            metadataHash,
            poolType,
            requiredGrowthLevel,
        },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendClaimReward(
    self,
    from: address,
    batchId: uint64,
    amount: coins,
    proof: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimReward { batchId, amount, proof },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendSetRewardJettonWallet(
    self,
    from: address,
    rewardJettonWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetRewardJettonWallet { rewardJettonWallet },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendSetRewardGrowthLevel(
    self,
    from: address,
    growthConfirmedLevel: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetRewardGrowthLevel { growthConfirmedLevel },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendSetRewardMigrationTargetAllowed(
    self,
    from: address,
    targetContract: address,
    allowed: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetRewardMigrationTargetAllowed { targetContract, allowed },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendSetRewardMigrationTarget(
    self,
    from: address,
    targetContract: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetRewardMigrationTarget { targetContract },
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.sendTransferRewardUncommittedHiToContract(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferRewardUncommittedHiToContract { amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MerkleRewardVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MerkleRewardVault.batchState(self, batchId: uint64): (
    uint256,
    coins,
    coins,
    uint256,
    uint8,
    uint8,
) {
    return net.runGetMethod(self.address, "batch_state", [batchId]);
}

fun MerkleRewardVault.hasClaimed(self, batchId: uint64, claimant: address): bool {
    return net.runGetMethod(self.address, "has_claimed", [batchId, claimant]);
}

fun MerkleRewardVault.poolState(self): (coins, coins, coins, coins, uint8) {
    return net.runGetMethod(self.address, "pool_state");
}
