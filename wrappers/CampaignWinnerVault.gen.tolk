// Auto-generated wrapper for contract 'CampaignWinnerVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/tokenomics-messages"
import "@contracts/tokenomics-storage"

/// Storage `CampaignWinnerStorage` is defined in `@contracts/tokenomics-storage`
struct CampaignWinnerVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun CampaignWinnerVault.fromStorage(
    storage: CampaignWinnerStorage,
    toShard: AddressShardingOptions? = null,
): CampaignWinnerVault {
    val stateInit = ContractState {
        code: build("CampaignWinnerVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return CampaignWinnerVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun CampaignWinnerVault.fromAddress(address: address): CampaignWinnerVault {
    return CampaignWinnerVault { address };
}

/// Deploys the contract to the blockchain
fun CampaignWinnerVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendRegisterCampaignWinner(
    self,
    from: address,
    winner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RegisterCampaignWinner { winner },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendClaimCampaignWinner(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimCampaignWinner {},
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendSetCampaignWinnerJettonWallet(
    self,
    from: address,
    winnerJettonWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetCampaignWinnerJettonWallet { winnerJettonWallet },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendSetGrowthConfirmedLevel(
    self,
    from: address,
    growthConfirmedLevel: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthConfirmedLevel { growthConfirmedLevel },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendSetCampaignMigrationTargetAllowed(
    self,
    from: address,
    targetContract: address,
    allowed: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetCampaignMigrationTargetAllowed { targetContract, allowed },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendSetCampaignMigrationTarget(
    self,
    from: address,
    targetContract: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetCampaignMigrationTarget { targetContract },
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.sendTransferCampaignUncommittedHiToContract(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferCampaignUncommittedHiToContract { amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CampaignWinnerVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CampaignWinnerVault.winnerState(self, winner: address): (bool, uint8, coins, coins) {
    return net.runGetMethod(self.address, "winner_state", [winner]);
}

fun CampaignWinnerVault.vaultState(self): (uint8, uint16, coins, coins) {
    return net.runGetMethod(self.address, "vault_state");
}

fun CampaignWinnerVault.claimable(self, winner: address): coins {
    return net.runGetMethod(self.address, "claimable", [winner]);
}

fun CampaignWinnerVault.growthConfirmedLevel(self): uint8 {
    return net.runGetMethod(self.address, "growth_confirmed_level");
}
