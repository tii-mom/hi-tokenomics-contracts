// Auto-generated wrapper for contract 'TeamVestingVault'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/tokenomics-messages"
import "@contracts/tokenomics-storage"

/// Storage `TeamVestingStorage` is defined in `@contracts/tokenomics-storage`
struct TeamVestingVault {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun TeamVestingVault.fromStorage(
    storage: TeamVestingStorage,
    toShard: AddressShardingOptions? = null,
): TeamVestingVault {
    val stateInit = ContractState {
        code: build("TeamVestingVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return TeamVestingVault { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun TeamVestingVault.fromAddress(address: address): TeamVestingVault {
    return TeamVestingVault { address };
}

/// Deploys the contract to the blockchain
fun TeamVestingVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendSyncTeamPriceLevel(
    self,
    from: address,
    teamConfirmedLevel: uint8,
    confirmedAt: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SyncTeamPriceLevel { teamConfirmedLevel, confirmedAt },
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendClaimTeam(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimTeam {},
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendSetTeamJettonWallet(
    self,
    from: address,
    teamJettonWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetTeamJettonWallet { teamJettonWallet },
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendSetTeamMigrationTargetAllowed(
    self,
    from: address,
    targetContract: address,
    allowed: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetTeamMigrationTargetAllowed { targetContract, allowed },
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendSetTeamMigrationTarget(
    self,
    from: address,
    targetContract: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetTeamMigrationTarget { targetContract },
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.sendTransferTeamUncommittedHiToContract(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferTeamUncommittedHiToContract { amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TeamVestingVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TeamVestingVault.vestingState(self): (uint8, coins, coins) {
    return net.runGetMethod(self.address, "vesting_state");
}
