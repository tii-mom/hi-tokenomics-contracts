// Auto-generated wrapper for contract 'PriceOracle'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/tokenomics-messages"
import "@contracts/tokenomics-storage"

/// Storage `PriceOracleStorage` is defined in `@contracts/tokenomics-storage`
struct PriceOracle {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun PriceOracle.fromStorage(
    storage: PriceOracleStorage,
    toShard: AddressShardingOptions? = null,
): PriceOracle {
    val stateInit = ContractState {
        code: build("PriceOracle"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return PriceOracle { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun PriceOracle.fromAddress(address: address): PriceOracle {
    return PriceOracle { address };
}

/// Deploys the contract to the blockchain
fun PriceOracle.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendReportGrowthPrice(
    self,
    from: address,
    price: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReportGrowthPrice { price },
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendConfirmGrowthLevel(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfirmGrowthLevel {},
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendReportTeamPrice(
    self,
    from: address,
    price: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ReportTeamPrice { price },
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendConfirmTeamLevel(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfirmTeamLevel {},
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendSetOracleTargets(
    self,
    from: address,
    growthEngine: address,
    teamVestingVault: address,
    merkleRewardVault: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetOracleTargets { growthEngine, teamVestingVault, merkleRewardVault },
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.sendSetCampaignWinnerTarget(
    self,
    from: address,
    campaignWinnerVault: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetCampaignWinnerTarget { campaignWinnerVault },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PriceOracle.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PriceOracle.growthConfirmedLevel(self): uint8 {
    return net.runGetMethod(self.address, "growth_confirmed_level");
}

fun PriceOracle.teamConfirmedLevel(self): uint8 {
    return net.runGetMethod(self.address, "team_confirmed_level");
}

fun PriceOracle.growthCandidateState(self): (uint8, uint32, uint64) {
    return net.runGetMethod(self.address, "growth_candidate_state");
}

fun PriceOracle.teamCandidateState(self): (uint8, uint32, uint64) {
    return net.runGetMethod(self.address, "team_candidate_state");
}

fun PriceOracle.growthThresholdFor(self, level: uint8): uint64 {
    return net.runGetMethod(self.address, "growth_threshold_for", [level]);
}

fun PriceOracle.teamThresholdFor(self, level: uint8): uint64 {
    return net.runGetMethod(self.address, "team_threshold_for", [level]);
}
