// Auto-generated wrapper for contract 'GrowthEngine'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/tokenomics-messages"
import "@contracts/tokenomics-storage"

/// Storage `GrowthEngineStorage` is defined in `@contracts/tokenomics-storage`
struct GrowthEngine {
    address: address
    stateInit: ContractState? = null
}

/// Creates a contract wrapper instance from the storage data
fun GrowthEngine.fromStorage(
    storage: GrowthEngineStorage,
    toShard: AddressShardingOptions? = null,
): GrowthEngine {
    val stateInit = ContractState {
        code: build("GrowthEngine"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { stateInit, toShard }.calculateAddress();
    return GrowthEngine { address, stateInit };
}

/// Creates a contract wrapper instance from the address
fun GrowthEngine.fromAddress(address: address): GrowthEngine {
    return GrowthEngine { address };
}

/// Deploys the contract to the blockchain
fun GrowthEngine.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: { stateInit: self.stateInit },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendBuyGrowth(
    self,
    from: address,
    tonAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BuyGrowth { tonAmount },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendClaimGrowth(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimGrowth {},
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSetGrowthEnabled(
    self,
    from: address,
    enabled: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthEnabled { enabled },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSetGrowthConfirmedLevel(
    self,
    from: address,
    growthConfirmedLevel: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthConfirmedLevel { growthConfirmedLevel },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendCloseGrowthEngine(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CloseGrowthEngine {},
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSweepGrowthProjectFund(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SweepGrowthProjectFund {},
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendWithdrawGrowthTons(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WithdrawGrowthTons { amount },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSetGrowthJettonWallet(
    self,
    from: address,
    growthJettonWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthJettonWallet { growthJettonWallet },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSetGrowthMigrationTargetAllowed(
    self,
    from: address,
    targetContract: address,
    allowed: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthMigrationTargetAllowed { targetContract, allowed },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendSetGrowthMigrationTarget(
    self,
    from: address,
    targetContract: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SetGrowthMigrationTarget { targetContract },
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.sendTransferGrowthUncommittedHiToContract(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TransferGrowthUncommittedHiToContract { amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GrowthEngine.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GrowthEngine.buyerState(self, owner: address): (coins, coins, uint8, coins, coins) {
    return net.runGetMethod(self.address, "buyer_state", [owner]);
}

fun GrowthEngine.growthState(self): (
    uint8,
    coins,
    coins,
    coins,
    coins,
    coins,
) {
    return net.runGetMethod(self.address, "growth_state");
}
